version: 2

sources:
  - name: bronze_agents
    description: table 
    columns:
      - name: agentid
        description:  A unique identifier for each agent.
        data_tests: 
          - not_null
      - name: is_contractor
        description:  flag to indicate whether the agent is a contractor.
        data_tests:
          - accepted_values:
            values: [true, false]
      - name: agent_fees
        description: agent fee
        
        
     